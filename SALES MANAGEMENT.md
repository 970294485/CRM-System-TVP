# 模塊 3：銷售管理 (SALES MANAGEMENT) 功能設計書

本模塊是系統的營收核心，負責管理從初步報價、正式合同到預收發票的完整商務流程。設計重點在於「數據自動流轉」與「模塊化串聯」。

---

## 零、 全局權限與特權 (Global Permissions)

* **超級管理員 (Super Admin God Mode)：**
    * **數據修正權：** 可直接修改已鎖定的報價單、合同或發票。修改後，系統將自動同步更新【模塊 1】的待收額度與【模塊 2】的會計分錄。
    * **模板控制權：** 可自定義、修改及刪除【模塊 4】中的所有導出模板。
    * **操作日誌：** 所有的「管理員覆蓋修改」動作必須在底層數據庫保留修改前、後的數值紀錄。

---

## 一、 業務功能詳細設計 (Functional Design)

### 1. 報價單功能 (Quotation Management)
* **新舊客戶兼容機制：**
    * **檢索現有客戶：** 輸入關鍵字自動從 **【模塊 6】** 獲取資料。
    * **快捷新建 (Quick Add)：** 若為新客戶，支持在報價介面直接錄入「客戶名稱、聯繫人、聯繫方式」。儲存報價單時，系統需執行**原子化操作**：先在【模塊 6】建立客戶，獲取 `customer_id` 後再保存報價單。
* **產品調用：** 關聯 **【模塊 8】** 產品庫，自動帶入單價、圖片、描述。
* **版本管理：** 支持同一客戶的多個報價版本（例如：v1, v2...），並記錄變更歷史。

### 2. 合同功能 (Contract Management)
* **一鍵轉化邏輯：** 報價單確認後，一鍵生成正式合同。數據 100% 繼承，並允許補充「支付條款」（如：預付 30%, 驗收 70%）。
* **附件管理：** 支援上傳雙方簽署後的合同掃描件，檔案自動上傳至 **【模塊 5】** 並與該客戶檔案關聯。

### 3. 預收發票功能 (Proforma Invoice)
* **財務連動：** 生成發票後，系統自動將「待收額度」推送到 **【模塊 1】** 的待匹配池。
* **狀態追蹤：** 銷售頁面需實時顯示該單據在【模塊 1】中的「收款確認狀態」。

### 4. 導出功能與模板選擇 (Export Service)
* **自定義模板：** 用戶點擊「導出」按鈕時，可從下拉選單選擇由 **【模塊 4】** 生成或自定義的模板。
* **自動文件流轉：**
    1. **數據傳輸：** 模塊 3 發送 JSON 數據給 **【模塊 4】**。
    2. **渲染生成：** 模塊 4 根據選擇的模板渲染 PDF。
    3. **自動歸檔：** 模塊 4 生成後自動將文件流傳送給 **【模塊 5】**。
    4. **下載與路徑：** 模塊 5 返回檔案下載連結，並根據 `Customer_ID` 自動分類存儲。

### 5. 分析報表功能 (Sales Analytics)
* **轉化率看板：** 統計從新客戶開發到正式成交的轉化率與平均週期。
* **應收帳款追蹤：** 讀取 **【模塊 1】** 數據，標註已發單但未付款的異常合同。

### 6. 對應庫單和採購對接功能 (Inventory & Procurement Sync)
* **庫存預扣：** 合同生效後，自動通知 **【模塊 8】** 預扣（佔用）庫存數量。
* **採購連動：** 若庫存不足，支持一鍵在 **【模塊 8】** 生成關聯採購單。

### 7. 對應財務和佣金功能 (Finance & Commission)
* **佣金試算：** 合同結清後，讀取 **【模塊 8】** 設定比例計算佣金，並在 **【模塊 2】** 生成費用分錄。

---

## 二、 跨模塊串聯邏輯 (Cross-Module Integration)

| 關聯模塊 | 數據流轉路徑 |
| :--- | :--- |
| **模塊 4 (文件導出)** | **生產端：** 接收模塊 3 數據並根據選定模板生成PDF。 |
| **模塊 5 (文件管理)** | **存儲端：** 接收模塊 4 產出的文件實體，為模塊 3 提供長期下載連結。 |
| **模塊 6 (客戶管理)** | **資料源：** 提供客戶信息；支持報價時「反向快捷建立」新客戶。 |
| **模塊 8 (資料輸入)** | **基礎數據：** 提供產品單價、庫存量、公司 Logo 及佣金比例。 |
| **模塊 1 (財務管理)** | **核銷端：** 接收發票數據進行收款確認；確認後反饋訂單狀態。 |
| **模塊 2 (會計管理)** | **記帳端：** 合同生效與佣金結算後，自動在本模塊生成會計分錄。 |

---

## 三、 給 Cursor 的開發指令 (Dev Notes)

1. **Transaction 處理：** "Ensure atomicity when creating a new quotation for a new customer. If customer creation in M6 fails, the quotation in M3 must not be saved."
2. **數據精度：** "All price, discount, and tax calculations must use `Decimal(15,2)`."
3. **模板接口：** "Module 3 should fetch available `template_list` from Module 4. When exporting, pass the `template_id` and the record JSON to the export endpoint."
4. **管理員覆蓋：** "For `SUPER_ADMIN` users, remove all `read-only` or `locked` constraints on finalized records. Upon saving, trigger a webhook/event to update linked records in M1 and M2."