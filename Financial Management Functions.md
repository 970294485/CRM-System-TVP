# 模塊 1：財務管理功能 (Financial Management Functions)

本模塊為系統的財務控管中心，負責記錄資金流轉狀態、核銷銷售合同與採購單，並執行預算監控與審批。本系統不直接執行外部支付動作，僅作為業務邏輯的紀錄與確認中心。

---

## 零、 全局權限與特權 (Global Permissions)

* **超級管理員 (Super Admin God Mode)：**
    * 擁有「強制修改權」：可無視單據鎖定狀態，直接修改已確認的收款/付款金額。
    * 擁有「流程跳過權」：可直接覆蓋審批流程，手動核銷任何合同或發票。
    * **系統紀錄：** 超級管理員的所有修改操作必須產生不可刪除的 `Audit Log` (審計日誌)。

---

## 一、 業務功能詳細設計 (Functional Design)

### 1. 管理請款單與預收款單 (Payment & Collection Records)
* **應付請款 (AP)：** * **掛鉤對象：** 必須與 **【模塊 8】的採購單 (Purchase Order)** 掛鉤。
    * **操作邏輯：** 財務人員在外部完成支付後，於系統內手動點擊「確認付款」，系統將採購單標記為「已支付」。
* **應收請款 (AR) 與預收款：**
    * **掛鉤對象：** 與 **【模塊 3】的銷售合同** 掛鉤。
    * **操作邏輯：** 財務手動錄入收到的款項（預收款單），用於後續的合同匹配。

### 2. 合同與發票預收款匹配 (Logical Matching)
* **自動同步機制：** 當 **【模塊 3】** 生成銷售合同或發票後，系統自動將「待收額度」同步至本模塊的待匹配清單。
* **匹配動作：** 財務人員手動將錄入的「預收款金額」分配給特定的「銷售合同」。
* **無需銀行流水：** 本功能純粹為數據勾稽。確認匹配後，系統自動更新模塊 3 的合同狀態為「已結清 (Paid)」，並解鎖 **【模塊 7】** 的服務排程。

### 3. 月度預算統計功能 (Monthly Budget Statistics)

* **統計基準：** 僅統計系統內已執行「確認」動作的入帳與出帳數據；月份歸屬依單據上的確認時間戳（請款：`confirmed_at`；預收款：`received_at`），預設以 UTC 自然月聚合，避免報表時區歧義。
* **數據彙整：**
    * **收入（當期實作）：** 「預收款單」狀態為「已確認收款」的金額，按月加總。與「合同／發票核銷額」完整對齊時，可另行疊加匹配模組的核銷明細視圖或 API。
    * **支出（當期實作）：** 「應付請款單」狀態為「已確認付款」的金額，按月加總；其掛鉤採購單來自模塊 8。
    * **人事參考成本：** 讀取模塊 8「人事數據」中**在職**員工的 `base_salary` 加總，作為「月度固定人工成本基準」並與當月已確認請款對照（非已發薪流水，待發薪紀錄表對接後可改為「實發」口徑）。
* **對比維度：**
    * **淨現金（確認口徑）：** 當月入帳預收款合計 − 當月出帳應付請款合計。
    * **與底薪基準差異參考：** 淨額 − 在職底薪合計（系統上以單一指標輔助觀察是否覆蓋固定人事成本；具體預算目標線待「預算收支智能管控」模組落地後接上）。
* **介面：** 側邊欄「月度預算統計功能」進入；支援切換統計年度、表格檢視 12 個月與全年合計；所有登入用戶可讀取，財務角色權限定義不影響本頁查看（編輯權限沿用其他財務單據）。
* **擴充路線：** 維護 `monthly_budget_targets`（部門／科目／金額）與達成率；核銷與預收分開透視；對接實發薪資與費用報銷確認單後納入同表。

### 4. 高效財務分析工具 (Financial Analytics)

* **定位：** 單頁儀表板，以系統內已存在之銷售合同、預收款單、採購單、應付請款單為數據源，**即時聚合**（非匯入銀行流水），供財務與管理層快速掌握「待收／待付」結構與資金壓力方向。
* **應收／預收看板：**
    * **合同待收餘額（總額口徑）：** 各合同 `total_amount` 減去該合同下所有狀態為「已確認收款」之預收款單金額加總，再於全公司加總；與「合同與發票預收款匹配」邏輯一致。
    * **預收目標待滿足：** 有設定 `prepayment_amount` 時，以「預收目標 − 已確認預收款」為餘額並加總。
    * **已確認／草稿預收款：** 全表 `Received` 與 `Draft` 預收款金額合計，用於觀察入帳進度與未確認曝險。
* **應付看板：**
    * **採購待付：** 各採購單 `total_amount - paid_amount`（僅計大於零者）加總；反映模塊 8 採購單上尚未走完付款確認之餘額。
    * **草稿應付請款：** 模組 1 狀態為 `Draft` 之應付請款單金額加總（尚未點「確認付款」）。
* **現金流透視（粗估）：**
    * **合同端待收** = 全公司合同待收餘額加總（與上列應收主指標一致）。
    * **採購端待付** = 採購單未付餘額加總。
    * **淨頭寸** = 待收 − 待付，僅作營運資金壓力之**方向性參考**，不作 IFRS／GAAP 現金流量表。
* **明細輔助：** 提供「合同待收 Top 10」「採購未付 Top 10」排序列表，並附導覽連結至匹配頁、採購單、請款與月度統計。
* **權限：** 與其他財務閱讀頁一致，登入可檢視；編輯仍限財務／管理角色於各單據頁操作。
* **擴充路線：** 帳齡區間（按合同日／採購日）、客戶／供應商維度下鑽、與會計模組科目餘額對賬、情境假設（回款週期、付款排程）與導出。

### 5. 預算收支智能管控 (Budget Control)

* **預算主數據：** 表 `finance_monthly_expenditure_budgets`，鍵為 `YYYY-MM`；欄位含 `cap_amount`（該月採購支出上限）、`is_active`（是否啟用攔截）、`notes`。遷移指令：`npm run db:apply:finance-expenditure-budget`（於 `web` 目錄）。
* **承諾額口徑（支出）：** 以 **採購單** `total_amount` 為準，按 `po_date` 所屬曆月加總（與是否已請款、已付款無關，代表當月「採購承諾」規模）。新增採購若使「該月既有承諾 + 本批次」超過上限則視為超限。
* **強控接入點（當期實作）：** **【模塊 8】** `POST /api/import-documents`、文件類型 `PURCHASE_ORDER`——在寫入資料庫前執行預算檢核；不依賴超級管理員手動核准流程，而是由 **角色 `super_admin` 自動繞過檢核**（等同規格所指「須超級管理員方可執行」之技術對應：僅該角色可強行匯入超支批次）。
* **管理介面：** 側邊欄「預算收支智能管控」`/dashboard/finance/budget-control`——按年度檢視 12 個月上限、開關、已建採購承諾、餘額；具財務編輯權者可儲存單月設定。
* **未覆蓋路徑：** 若日後開放「非匯入」之採購單 API，須調用同一套 `evaluatePurchaseOrderImportAgainstBudget` 邏輯（抽出為共用服務）；**報銷單**尚未入表時預留與科目／部門預算池合併。
* **擴充路線：** 分科目／部門維度預算、與草稿請款試算連動、超支申請工單（非僅角色繞過）、收入側預算與実績對比。

### 6. 設置多層審批權限 (Multi-level Approval Workflow)

* **策略主數據：** 表 `finance_approval_policies`（名稱、單據類型、金額門檻、啟用、有序 `steps` JSON：每階含 `label` + `roleSlugs`）。遷移：`npm run db:apply:finance-approval`（於 `web` 目錄）。
* **審批紀錄：** `finance_approval_events`——每筆單據、每個 `step_index` 至多一答已簽紀錄（`document_type` + `document_id` + `step_index` 唯一）。
* **適用規則（應付請款 AP_PAYMENT）：** 依請款**金額**選擇**啟用中**策略裡，`amount_min` **最大且不超過**該金額的那一條；若無任一符合或 `steps` 為空，則不要求多階簽核即可「確認付款」。
* **職級聯動：** 各階 `roleSlugs` 對應模組 9 **`roles.slug`**；登入使用者任一角與該階任一 slug 交集即可呼叫「核准當前階段」。 **`super_admin` 角色**可直接「確認付款」並可直接簽任一步（與規格中超級管理員特權一致）。
* **介面：** 側邊欄「設置多層審批權限」`/dashboard/finance/multi-level-approval`——財務可新增／刪除策略並勾選每階可簽角色；「管理請款單」列表顯示各階狀態、提供「核准當前階段」／「確認付款」（未完成審批時後者禁用，後端 `PATCH` 亦擋）。
* **擴充路線：** 預算調整、預收款等單據類型；可編輯策略（非僅刪除重建）；審計日誌與駁回／重送流程。

---

## 二、 跨模塊串聯邏輯 (Cross-Module Integration)

| 關聯模塊 | 數據流轉與觸發動作 |
| :--- | :--- |
| **模塊 3 (銷售管理)** | **自動推送：** 合同生成後，自動在模塊 1 產生「待匹配指標」。<br>**狀態反饋：** 模塊 1 確認匹配後，更新模塊 3 單據狀態為「已付款」。 |
| **模塊 8 (資料輸入)** | **採購對接：** 模塊 1 的應付請款必須檢核模塊 8 的採購單編號與金額。<br>**預算基準：** 預算管控需調用模塊 8 的人事與產品成本數據。 |
| **模塊 2 (會計管理)** | **會計分錄：** 模塊 1 每次點擊「確認付款/收款」，自動在模塊 2 生成一張會計傳票。 |
| **模塊 4 (文件導出)** | **單據渲染：** 調用模塊 4 生成 `PAYMENT REQUEST` (請款單) 的 PDF 文件。 |
| **模塊 7 (服務管理)** | **排程解鎖：** 財務確認收款匹配後，發送信號解鎖模塊 7 的場地與員工預約功能。 |
| **模塊 5 (文件管理)** | **檔案歸檔：** 收款/付款憑證圖片自動存儲於模塊 5 的客戶/供應商專屬目錄。 |
| **模塊 9 (系統設置)** | **權限校驗：** 模塊 1 的多層審批與超級管理員特權需校驗模塊 9 的 Role-Based Access Control (RBAC)。 |

---

## 三、 給 Cursor 的開發指令 (Dev Notes)

1. **數據庫設計：** - `FinanceMatch` 表必須紀錄 `contract_id` 與 `payment_record_id` 的關聯。
   - 確保 `amount` 字段使用 `Decimal(15,2)` 類型以保證精度。
2. **超級管理員邏輯：** 在後端修改 API 中增加判斷：若當前用戶為 `SUPER_ADMIN`，則跳過所有單據狀態 (Status) 的修改限制。
3. **無異步銀行對接：** 不需要實作 Webhook 或外部銀行接口，所有確認動作均由用戶在前端觸發。